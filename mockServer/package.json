{
  "name": "mockServer",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "json-server -p 3001 -w db.json --delay 2000"
  },
  "dependencies": {
    "json-server": "^0.16.3"
  }
}
